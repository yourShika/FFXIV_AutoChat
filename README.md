<h1>AutoChat 🌟</h1>

<p>
  A lightweight Dalamud plugin that periodically sends a custom message to a selected chat channel
  (<strong>Say</strong> / <strong>Party</strong> / <strong>Alliance</strong> / <strong>Yell</strong> / <strong>Shout</strong> / <strong>Free Company</strong>).
  Includes a simple configuration window with <em>Start/Stop</em> controls. 🚀
</p>

<hr />

<h2>✨ Features</h2>
<ul>
  <li>📝 Customizable message text</li>
  <li>⏱️ Adjustable interval (seconds)</li>
  <li>🗨️ Channel selection: Say / Party / Alliance / Yell / Shout / Free Company</li>
  <li>▶️ Start / ⏹️ Stop buttons + “Send now” action</li>
</ul>

<h2>📦 Requirements</h2>
<ul>
  <li>.NET SDK 8.0 (or compatible)</li>
  <li>Dalamud / XIVLauncher (Dev Plugins enabled)</li>
</ul>

<h2>🧰 Project Setup</h2>
<ol>
  <li>Create a new <strong>Class Library (.NET)</strong> project, or use the CLI.</li>
  <li>Use the <strong>Dalamud.NET.Sdk</strong> in your <code>.csproj</code> (no extra packager needed).</li>
  <li>Place <code>DalamudPluginManifest.json</code> in the project root.</li>
</ol>

<h3>Example <code>.csproj</code></h3>
<pre><code>&lt;Project Sdk="Dalamud.NET.Sdk/13.1.0"&gt;
  &lt;PropertyGroup&gt;
    &lt;TargetFramework&gt;net8.0-windows7.0&lt;/TargetFramework&gt;
    &lt;Version&gt;1.0.0.0&lt;/Version&gt;
    &lt;Description&gt;AutoChat – periodic chat messages&lt;/Description&gt;
    &lt;Nullable&gt;enable&lt;/Nullable&gt;
    &lt;ImplicitUsings&gt;enable&lt;/ImplicitUsings&gt;
    &lt;IsPackable&gt;false&lt;/IsPackable&gt;
  &lt;/PropertyGroup&gt;

  &lt;ItemGroup&gt;
    &lt;None Include="DalamudPluginManifest.json"&gt;
      &lt;CopyToOutputDirectory&gt;PreserveNewest&lt;/CopyToOutputDirectory&gt;
    &lt;/None&gt;
  &lt;/ItemGroup&gt;
&lt;/Project&gt;
</code></pre>

<h2>📁 Suggested Structure</h2>
<pre><code>AutoChat/
├─ AutoChat.csproj
├─ DalamudPluginManifest.json
├─ Plugin.cs
├─ Configuration.cs
└─ Ui/AutoChatWindow.cs
</code></pre>

<h2>⚙️ Build</h2>
<ol>
  <li>Open a terminal in the project directory.</li>
  <li>Run: <code>dotnet build -c Release</code></li>
  <li>Output ZIP will be under <code>bin/Release</code> (generated by the SDK).</li>
</ol>

<h2>🧪 Install as Dev Plugin</h2>
<ol>
  <li>Copy the built ZIP to <code>%AppData%/XIVLauncher/devPlugins</code>.</li>
  <li>Start XIVLauncher → Dalamud → Dev Plugins → enable <strong>AutoChat</strong>.</li>
</ol>

<h2>🕹️ Usage</h2>
<ul>
  <li>Use the command <code>/autochat</code> to open the settings window.</li>
  <li>Set your message, interval (seconds), and target channel.</li>
  <li>Click <em>Start</em> to begin or <em>Send now</em> to immediately send once.</li>
</ul>

<h2>🔤 Chat Channels</h2>
<table>
  <thead>
    <tr>
      <th>Channel</th>
      <th>Command Prefix</th>
    </tr>
  </thead>
  <tbody>
    <tr><td>Say</td><td><code>/s</code></td></tr>
    <tr><td>Party</td><td><code>/p</code></td></tr>
    <tr><td>Alliance</td><td><code>/a</code></td></tr>
    <tr><td>Yell</td><td><code>/y</code></td></tr>
    <tr><td>Shout</td><td><code>/sh</code></td></tr>
    <tr><td>Free Company</td><td><code>/fc</code></td></tr>
  </tbody>
</table>

<h2>🧩 Code Notes</h2>
<ul>
  <li>Prefer sending via <code>ICommandManager.ProcessCommand()</code> (e.g., <code>/p Hello</code>) for channel delivery.</li>
  <li><code>IChatGui.Print()</code> is useful for local status messages.</li>
  <li>Ensure Windows TFM (e.g., <code>net8.0-windows7.0</code>) to satisfy platform APIs.</li>
  <li>Avoid duplicate <code>PackageReference</code> entries (no separate packager needed with the SDK).</li>
</ul>

<h2>🚦 Anti-Spam & Etiquette</h2>
<p>
  Please respect server and community rules. Keep intervals reasonable and avoid repetitive or disruptive messages. 🙏
</p>

<h2>🧭 Roadmap Ideas</h2>
<ul>
  <li>🔁 Rotate across multiple messages</li>
  <li>🕒 Time windows (e.g., only 18:00–22:00)</li>
  <li>🧯 Per-channel cooldowns</li>
  <li>🧪 Logging last send time</li>
  <li>💬 Support for tells/linkshells/cwls (with target)</li>
</ul>

<h2>🛠 Troubleshooting</h2>
<ul>
  <li><strong>“IChatGui has no SendMessage”</strong>: use <code>CommandManager.ProcessCommand()</code> to send channel messages.</li>
  <li><strong>Platform API warnings</strong>: ensure <code>&lt;TargetFramework&gt;net8.0-windows7.0&lt;/TargetFramework&gt;</code>.</li>
  <li><strong>Nullability errors</strong>: make late-assigned fields nullable and guard saves.</li>
</ul>

<h2>📜 License</h2>
<p>MIT (or your choice). 🧾</p>

<hr />

<p>Made with ❤️ for the FFXIV community.</p>
